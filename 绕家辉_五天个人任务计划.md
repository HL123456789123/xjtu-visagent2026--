# 绕家辉：五天个人任务计划（V1 执行版）

> 角色：食物识别后端、数据库和 Repository
> 唯一接口标准：`docs/contracts/api_v1.md`。发现冲突只提交问题，不得私自修改对外字段；模块内部辅助字段允许增加。

## 开始前统一原则

已有代码先运行和核对，不直接推倒重写。对外接口与 V1 不一致时优先增加 Adapter；创新只能放在模块内部，必须可关闭或回滚，且不得阻塞 Day3/Day4。需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。

## 1. 我的总体职责

实现同步 Food API、模型适配、ORM、唯一 migration 和 V1 Repository。

- 实现 `backend/app/api/food.py` 和 `FoodRecognitionService`。
- 把 `ModelDetection` 转成 `IngredientCandidate`。
- 实现 Mock/Yolo Provider，但不改变 V1 Python 接口。
- 保存图片到 MinIO，保存 raw 和 confirmed ingredients。
- 唯一修改 ORM 和 Alembic migration。
- 实现 Food、Recipe、Chat Repository，陈煜君不直接写 SQLAlchemy。

## 2. 我负责的文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/app/api/food.py | 建议新增 | 同步 Food API |
| backend/app/entity/food_schemas.py | 建议新增 | Food DTO |
| backend/app/services/food_recognition_service.py | 建议新增 | 图片、模型和持久化流程 |
| backend/app/repositories/food_repository.py | 建议新增 | Food 数据访问 |
| backend/app/repositories/recipe_repository.py | 建议新增 | Recipe 数据访问 |
| backend/app/repositories/chat_repository.py | 建议新增 | Chat 数据访问 |
| backend/app/entity/db_models.py | 现有 | 三张表和 `chat_sessions.recipe_id` |
| backend/alembic/versions/ | 现有目录 | 唯一 migration |

## 3. 我不能直接修改的文件

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/main.py | 闫灿宇接路由 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| backend/app/services/chat_service.py | 陈煜君负责 |
| frontend/ | 不修改前端 |
| docker-compose.yml | 吴雯负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

## 4. 我必须遵守的接口

- Food API
- FoodRecognitionProvider
- Food/Recipe/Chat Repository
- V1 数据库字段
- canonical fixtures

## 5. 五天总览

| 天数 | 主题 | 我的关键输出 |
| --- | --- | --- |
| 第 1 天 | 领取冻结接口，建立模块骨架 | Food Schema、Repository 骨架、Mock Provider |
| 第 2 天 | 按固定输入输出完成核心模块 | 可运行 Food API、migration、Repository |
| 第 3 天 | Mock 全链路 | 持久化记录、用户隔离结果、Mock E2E |
| 第 4 天 | 真实模型与真实 LLM | 真实 Food API、503 响应、Docker 日志 |
| 第 5 天 | 回归、Docker 与答辩证据 | 复测结果、数据库截图、五天总结 |

## 第 1 天：领取冻结接口，建立模块骨架

### 今天直接做什么

按 V1 建 Food Schema、Provider、Repository 和 ORM 骨架。

### 开始前读取

- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《03-后端项目初始化.pdf》路由和 Service
- 《05-用户认证功能.pdf》current_user
- 《06-目标检测功能.pdf》图片与检测
- V1 第二至五、九至十三节

### 固定输入

- V1 第三至五、九至十节
- canonical fixture

### P0：今天必须完成

1. 建立 `food_schemas.py`，字段完全复制 V1。
2. 建立固定同步 Provider 接口和 Mock 返回。
3. 建立三个 Repository 文件和 ORM 变更草案。

### P1：原则上完成

- 准备唯一 migration。
- 加入用户隔离方法骨架。

### P2：有时间再做

- 补类型注解。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/app/api/food.py | 建议新增 | 同步 Food API |
| backend/app/entity/food_schemas.py | 建议新增 | Food DTO |
| backend/app/services/food_recognition_service.py | 建议新增 | 图片、模型和持久化流程 |
| backend/app/repositories/food_repository.py | 建议新增 | Food 数据访问 |
| backend/app/repositories/recipe_repository.py | 建议新增 | Recipe 数据访问 |
| backend/app/repositories/chat_repository.py | 建议新增 | Chat 数据访问 |
| backend/app/entity/db_models.py | 现有 | 三张表和 `chat_sessions.recipe_id` |
| backend/alembic/versions/ | 现有目录 | 唯一 migration |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/main.py | 闫灿宇接路由 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| backend/app/services/chat_service.py | 陈煜君负责 |
| frontend/ | 不修改前端 |
| docker-compose.yml | 吴雯负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- Food Schema
- Repository 骨架
- Mock Provider

### 测试和证据

- `uv run pytest`。
- Schema 能解析 canonical fixture。
- 把实际命令、结果和截图写入 `docs/daily_logs/绕家辉/day1.md`。

### 对接与交付

不向陈煜君要 SQL，由 Repository 提供固定方法。

- 在固定分支 `feature/backend-food-recognition` 提交真实改动；集成分支只使用 `integration/day3-mock` 和 `integration/day4-real`。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
项目：食物识别生成菜谱智能体平台，第 1 天，角色：食物识别后端、数据库和 Repository。
先阅读 docs/contracts/api_v1.md，它是唯一接口标准。
今日目标：按 V1 建 Food Schema、Provider、Repository 和 ORM 骨架。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/绕家辉/day1.md`。

## 第 2 天：按固定输入输出完成核心模块

### 今天直接做什么

完成同步 Food API、ORM、唯一 migration 和 Repository。

### 开始前读取

- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《03-后端项目初始化.pdf》路由和 Service
- 《05-用户认证功能.pdf》current_user
- 《06-目标检测功能.pdf》图片与检测
- V1 第二至五、九至十三节

### 固定输入

- Mock Provider
- current_user
- canonical fixture

### P0：今天必须完成

1. 实现 POST/GET/PUT Food API。
2. 实现 MinIO 保存、raw detections 和 confirmed ingredients。
3. 生成唯一 Alembic migration。

### P1：原则上完成

- 补 401/403/404/413/415/422。
- 完成 Recipe/Chat Repository 方法。

### P2：有时间再做

- 增加索引优化。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/app/api/food.py | 建议新增 | 同步 Food API |
| backend/app/entity/food_schemas.py | 建议新增 | Food DTO |
| backend/app/services/food_recognition_service.py | 建议新增 | 图片、模型和持久化流程 |
| backend/app/repositories/food_repository.py | 建议新增 | Food 数据访问 |
| backend/app/repositories/recipe_repository.py | 建议新增 | Recipe 数据访问 |
| backend/app/repositories/chat_repository.py | 建议新增 | Chat 数据访问 |
| backend/app/entity/db_models.py | 现有 | 三张表和 `chat_sessions.recipe_id` |
| backend/alembic/versions/ | 现有目录 | 唯一 migration |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/main.py | 闫灿宇接路由 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| backend/app/services/chat_service.py | 陈煜君负责 |
| frontend/ | 不修改前端 |
| docker-compose.yml | 吴雯负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- 可运行 Food API
- migration
- Repository

### 测试和证据

- `uv run pytest`。
- 用 fixture 测 201、200、空食材和越权。
- 把实际命令、结果和截图写入 `docs/daily_logs/绕家辉/day2.md`。

### 对接与交付

把 Repository 方法交陈煜君直接调用。

- 在固定分支 `feature/backend-food-recognition` 提交真实改动；集成分支只使用 `integration/day3-mock` 和 `integration/day4-real`。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
项目：食物识别生成菜谱智能体平台，第 2 天，角色：食物识别后端、数据库和 Repository。
先阅读 docs/contracts/api_v1.md，它是唯一接口标准。
今日目标：完成同步 Food API、ORM、唯一 migration 和 Repository。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/绕家辉/day2.md`。

## 第 3 天：Mock 全链路

### 今天直接做什么

接 PostgreSQL、MinIO 和 Mock Provider，支持 Mock 全链路。

### 开始前读取

- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《03-后端项目初始化.pdf》路由和 Service
- 《05-用户认证功能.pdf》current_user
- 《06-目标检测功能.pdf》图片与检测
- V1 第二至五、九至十三节

### 固定输入

- Mock Provider
- PostgreSQL
- MinIO

### P0：今天必须完成

1. 持久化 recognition 和 confirmed ingredients。
2. 保证只能读取当前用户记录。
3. 让 RecipeRepository 可创建 version=1 并保存 JSON。

### P1：原则上完成

- 补 MinIO 失败处理。
- 配合吴雯跑 E2E。

### P2：有时间再做

- 补更多隔离测试。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/app/api/food.py | 建议新增 | 同步 Food API |
| backend/app/entity/food_schemas.py | 建议新增 | Food DTO |
| backend/app/services/food_recognition_service.py | 建议新增 | 图片、模型和持久化流程 |
| backend/app/repositories/food_repository.py | 建议新增 | Food 数据访问 |
| backend/app/repositories/recipe_repository.py | 建议新增 | Recipe 数据访问 |
| backend/app/repositories/chat_repository.py | 建议新增 | Chat 数据访问 |
| backend/app/entity/db_models.py | 现有 | 三张表和 `chat_sessions.recipe_id` |
| backend/alembic/versions/ | 现有目录 | 唯一 migration |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/main.py | 闫灿宇接路由 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| backend/app/services/chat_service.py | 陈煜君负责 |
| frontend/ | 不修改前端 |
| docker-compose.yml | 吴雯负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- 持久化记录
- 用户隔离结果
- Mock E2E

### 测试和证据

- `uv run pytest`。
- 两个用户交叉访问返回 HTTP 403 `FORBIDDEN`。
- 把实际命令、结果和截图写入 `docs/daily_logs/绕家辉/day3.md`。

### 对接与交付

先交数据层 PR，再让陈煜君接服务。

- 在固定分支 `feature/backend-food-recognition` 提交真实改动；集成分支只使用 `integration/day3-mock` 和 `integration/day4-real`。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
项目：食物识别生成菜谱智能体平台，第 3 天，角色：食物识别后端、数据库和 Repository。
先阅读 docs/contracts/api_v1.md，它是唯一接口标准。
今日目标：接 PostgreSQL、MinIO 和 Mock Provider，支持 Mock 全链路。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/绕家辉/day3.md`。

## 第 4 天：真实模型与真实 LLM

### 今天直接做什么

接黄小石真实模型并把不可用转换为 503。

### 开始前读取

- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《03-后端项目初始化.pdf》路由和 Service
- 《05-用户认证功能.pdf》current_user
- 《06-目标检测功能.pdf》图片与检测
- V1 第二至五、九至十三节

### 固定输入

- best.pt 本地路径
- classes.yaml
- 固定图

### P0：今天必须完成

1. 从环境变量读取模型和 classes 路径。
2. 把 ModelDetection 转成 IngredientCandidate。
3. 捕获 FoodModelUnavailableError 并返回 FOOD_MODEL_UNAVAILABLE。

### P1：原则上完成

- 记录 model_version 和 provider=yolo。
- 在 Docker 中验证权重挂载。

### P2：有时间再做

- 记录推理耗时。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/app/api/food.py | 建议新增 | 同步 Food API |
| backend/app/entity/food_schemas.py | 建议新增 | Food DTO |
| backend/app/services/food_recognition_service.py | 建议新增 | 图片、模型和持久化流程 |
| backend/app/repositories/food_repository.py | 建议新增 | Food 数据访问 |
| backend/app/repositories/recipe_repository.py | 建议新增 | Recipe 数据访问 |
| backend/app/repositories/chat_repository.py | 建议新增 | Chat 数据访问 |
| backend/app/entity/db_models.py | 现有 | 三张表和 `chat_sessions.recipe_id` |
| backend/alembic/versions/ | 现有目录 | 唯一 migration |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/main.py | 闫灿宇接路由 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| backend/app/services/chat_service.py | 陈煜君负责 |
| frontend/ | 不修改前端 |
| docker-compose.yml | 吴雯负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- 真实 Food API
- 503 响应
- Docker 日志

### 测试和证据

- 固定图 POST 测试。
- 移走权重后验证 503。
- 把实际命令、结果和截图写入 `docs/daily_logs/绕家辉/day4.md`。

### 对接与交付

把真实接口结果交刘楚涵和吴雯。

- 在固定分支 `feature/backend-food-recognition` 提交真实改动；集成分支只使用 `integration/day3-mock` 和 `integration/day4-real`。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
项目：食物识别生成菜谱智能体平台，第 4 天，角色：食物识别后端、数据库和 Repository。
先阅读 docs/contracts/api_v1.md，它是唯一接口标准。
今日目标：接黄小石真实模型并把不可用转换为 503。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/绕家辉/day4.md`。

## 第 5 天：回归、Docker 与答辩证据

### 今天直接做什么

修复数据层和 Food 后端问题。

### 开始前读取

- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《03-后端项目初始化.pdf》路由和 Service
- 《05-用户认证功能.pdf》current_user
- 《06-目标检测功能.pdf》图片与检测
- V1 第二至五、九至十三节

### 固定输入

- release candidate
- 缺陷清单

### P0：今天必须完成

1. 回归三条 Food API。
2. 回归 migration、用户隔离和 Repository。
3. 修复分配的 P0/P1。

### P1：原则上完成

- 整理接口响应和数据库证据。
- 完成个人总结。

### P2：有时间再做

- 不增加新表。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/app/api/food.py | 建议新增 | 同步 Food API |
| backend/app/entity/food_schemas.py | 建议新增 | Food DTO |
| backend/app/services/food_recognition_service.py | 建议新增 | 图片、模型和持久化流程 |
| backend/app/repositories/food_repository.py | 建议新增 | Food 数据访问 |
| backend/app/repositories/recipe_repository.py | 建议新增 | Recipe 数据访问 |
| backend/app/repositories/chat_repository.py | 建议新增 | Chat 数据访问 |
| backend/app/entity/db_models.py | 现有 | 三张表和 `chat_sessions.recipe_id` |
| backend/alembic/versions/ | 现有目录 | 唯一 migration |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/main.py | 闫灿宇接路由 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| backend/app/services/chat_service.py | 陈煜君负责 |
| frontend/ | 不修改前端 |
| docker-compose.yml | 吴雯负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- 复测结果
- 数据库截图
- 五天总结

### 测试和证据

- `uv run pytest`。
- Docker Food 主流程。
- 把实际命令、结果和截图写入 `docs/daily_logs/绕家辉/day5.md`。

### 对接与交付

复测通过后交吴雯。

- 在固定分支 `feature/backend-food-recognition` 提交真实改动；集成分支只使用 `integration/day3-mock` 和 `integration/day4-real`。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
项目：食物识别生成菜谱智能体平台，第 5 天，角色：食物识别后端、数据库和 Repository。
先阅读 docs/contracts/api_v1.md，它是唯一接口标准。
今日目标：修复数据层和 Food 后端问题。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/绕家辉/day5.md`。

## 五天结束时必须交付

- 负责模块的源码和测试。
- day1 到 day5 个人日志。
- Commit、PR 或 Draft PR 记录。
- 页面、API、模型、SSE 或 Docker 证据。
- 能用自己的话说明输入、输出、错误处理和本人修改内容。

## 答辩时必须会讲

- 自己的角色和文件边界。
- V1 中与自己有关的固定输入输出。
- 自己实际运行的测试和结果。
- AI 工具做了什么、本人检查和修改了什么。
- 未完成或降级到 P2 的内容。
