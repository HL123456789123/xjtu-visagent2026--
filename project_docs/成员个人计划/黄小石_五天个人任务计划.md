# 黄小石：五天个人任务计划（V1 执行版）

> 角色：AI / 食物识别模型
> 唯一接口标准：`docs/contracts/api_v1.md`。发现冲突只提交问题，不得私自修改对外字段；模块内部辅助字段允许增加。

## 开始前统一原则

根目录 `AGENTS.md` 负责环境和命令，`docs/contracts/api_v1.md` 负责接口，本计划只负责任务、Owner、文件边界、优先级和交付物。后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。本文保留的命令只作任务验收提示，工作目录和最终命令以 `AGENTS.md` 为准；不得声称未执行的测试已通过，完成后必须报告实际工作目录、命令、结果和风险。

Day3 和 Day4 直接在 `develop` 联调，用集成记录和对应 Commit 哈希标记阶段状态；不创建额外 `integration/*` 分支。个人临时分支通过 PR 合入 `develop` 后及时删除，`master` 只用于最终稳定版本。

已有代码先运行和核对，不直接推倒重写。对外接口与 V1 不一致时优先增加 Adapter；创新只能放在模块内部，必须可关闭或回滚，且不得阻塞 Day3/Day4。需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。

## 1. 我的总体职责

在租用的 GPU 服务器独立训练最稳定的 12 类，并交付固定 `ModelDetection` 接口。

- 根据样本量和框标注质量独立选择最稳定的 12 类。
- 提交 `classes.yaml`，其他成员直接读取，不再讨论类别或训练参数。
- 在租用的 GPU 服务器训练和评估 YOLOv11。
- 实现输入 `image_path`、`conf_threshold=0.25` 的统一推理接口。
- 返回 `list[ModelDetection]`；无结果返回 `[]`；不可用抛出 `FoodModelUnavailableError`。
- 记录 precision、recall、mAP 和推理时间；`best.pt` 不进入 Git。

## 2. 我负责的文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/scripts/food_model/classes.yaml | 建议新增 | 12 类及中英文映射 |
| backend/scripts/food_model/prepare_dataset.py | 建议新增 | 数据转换和检查 |
| backend/scripts/food_model/train.py | 建议新增 | GPU 服务器训练入口 |
| backend/scripts/food_model/evaluate.py | 建议新增 | 固定图和指标评估 |
| backend/scripts/food_model/README.md | 建议新增 | 文件名、类别顺序、用法和限制 |
| backend/app/modeling/food_yolo_runtime.py | 建议新增 | 统一模型运行时 |

## 3. 我不能直接修改的文件

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/app/api/food.py | 绕家辉负责 |
| backend/app/entity/db_models.py | 绕家辉负责 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| frontend/ | 不修改前端 |
| docker-compose.yml | 吴雯负责挂载 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

## 4. 我必须遵守的接口

- FoodRecognitionProvider
- ModelDetection
- BoundingBox
- FoodModelUnavailableError
- classes.yaml

## 5. 五天总览

| 天数 | 主题 | 我的关键输出 |
| --- | --- | --- |
| 第 1 天 | 领取冻结接口，建立模块骨架 | 12 类 classes.yaml、模型接口骨架、固定图清单 |
| 第 2 天 | 按固定输入输出完成核心模块 | 首轮指标、训练脚本、接口测试结果 |
| 第 3 天 | Mock 全链路 | 固定图报告、模型运行说明、候选 best.pt 本地位置 |
| 第 4 天 | 真实模型与真实 LLM | 最终指标、本地加载说明、真实接口结果 |
| 第 5 天 | 回归、Docker 与答辩证据 | 模型报告、截图、五天总结 |

## 第 1 天：领取冻结接口，建立模块骨架

### 今天直接做什么

在租用 GPU 服务器上独立选定最稳定的 12 类并提交类别文件。

### 开始前读取

- 根目录 `AGENTS.md`：先确认工作目录、包管理器、实际命令和 Agent 执行限制。
- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《06-目标检测功能.pdf》YOLO 推理
- 《07-模型训练功能.pdf》训练和评估
- V1 第三、四节

### 固定输入

- 现有带框数据
- V1 第三、四节

### P0：今天必须完成

1. 检查框标注和每类样本量，独立选 12 类。
2. 提交固定格式 `classes.yaml`。
3. 建立 `FoodRecognitionProvider`、`ModelDetection` 和异常接口测试。

### P1：原则上完成

- 准备 3 到 5 张固定演示图。
- 记录数据限制。

### P2：有时间再做

- 准备额外候选类，只作备选。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/scripts/food_model/classes.yaml | 建议新增 | 12 类及中英文映射 |
| backend/scripts/food_model/prepare_dataset.py | 建议新增 | 数据转换和检查 |
| backend/scripts/food_model/train.py | 建议新增 | GPU 服务器训练入口 |
| backend/scripts/food_model/evaluate.py | 建议新增 | 固定图和指标评估 |
| backend/scripts/food_model/README.md | 建议新增 | 文件名、类别顺序、用法和限制 |
| backend/app/modeling/food_yolo_runtime.py | 建议新增 | 统一模型运行时 |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/app/api/food.py | 绕家辉负责 |
| backend/app/entity/db_models.py | 绕家辉负责 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| frontend/ | 不修改前端 |
| docker-compose.yml | 吴雯负责挂载 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- 12 类 classes.yaml
- 模型接口骨架
- 固定图清单

### 测试和证据

- 检查每类都有 class_name/display_name。
- 空结果为 []，异常为 FoodModelUnavailableError。
- 把实际命令、结果和截图写入 `docs/daily_logs/黄小石/day1.md`。

### 对接与交付

其他成员直接读取 classes.yaml，不讨论训练参数。

- 在固定分支 `feature/model-food-yolo` 提交真实改动；个人临时分支通过 PR 合入 `develop`，合并后及时删除。Day3 和 Day4 直接在 `develop` 联调，不再创建额外 integration 分支。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
开始任务前必须先阅读：
1. 根目录 `AGENTS.md`；
2. `docs/contracts/api_v1.md`；
3. 本人的个人任务计划。
`AGENTS.md` 负责环境和命令；`api_v1.md` 负责接口。
后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。
不得声称未执行的测试已通过；完成后必须报告实际工作目录、实际命令、真实结果、修改文件和风险。
项目：食物识别生成菜谱智能体平台，第 1 天，角色：AI / 食物识别模型。
今日目标：在租用 GPU 服务器上独立选定最稳定的 12 类并提交类别文件。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/黄小石/day1.md`。

## 第 2 天：按固定输入输出完成核心模块

### 今天直接做什么

在租用 GPU 服务器完成首轮训练。

### 开始前读取

- 根目录 `AGENTS.md`：先确认工作目录、包管理器、实际命令和 Agent 执行限制。
- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《06-目标检测功能.pdf》YOLO 推理
- 《07-模型训练功能.pdf》训练和评估
- V1 第三、四节

### 固定输入

- 12 类数据
- 训练脚本
- GPU 服务器

### P0：今天必须完成

1. 运行数据检查和 YOLOv11 训练。
2. 记录 precision、recall、mAP 和推理时间。
3. 用统一接口加载首轮权重并返回 bbox 对象。

### P1：原则上完成

- 保存训练配置和日志。
- 分析最差类别。

### P2：有时间再做

- 尝试一次轻量调参。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/scripts/food_model/classes.yaml | 建议新增 | 12 类及中英文映射 |
| backend/scripts/food_model/prepare_dataset.py | 建议新增 | 数据转换和检查 |
| backend/scripts/food_model/train.py | 建议新增 | GPU 服务器训练入口 |
| backend/scripts/food_model/evaluate.py | 建议新增 | 固定图和指标评估 |
| backend/scripts/food_model/README.md | 建议新增 | 文件名、类别顺序、用法和限制 |
| backend/app/modeling/food_yolo_runtime.py | 建议新增 | 统一模型运行时 |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/app/api/food.py | 绕家辉负责 |
| backend/app/entity/db_models.py | 绕家辉负责 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| frontend/ | 不修改前端 |
| docker-compose.yml | 吴雯负责挂载 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- 首轮指标
- 训练脚本
- 接口测试结果

### 测试和证据

- 运行训练和评估脚本。
- 校验 class_name 与 classes.yaml 一致。
- 把实际命令、结果和截图写入 `docs/daily_logs/黄小石/day2.md`。

### 对接与交付

只交接口结果和类别顺序，不要求他人评审训练参数。

- 在固定分支 `feature/model-food-yolo` 提交真实改动；个人临时分支通过 PR 合入 `develop`，合并后及时删除。Day3 和 Day4 直接在 `develop` 联调，不再创建额外 integration 分支。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
开始任务前必须先阅读：
1. 根目录 `AGENTS.md`；
2. `docs/contracts/api_v1.md`；
3. 本人的个人任务计划。
`AGENTS.md` 负责环境和命令；`api_v1.md` 负责接口。
后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。
不得声称未执行的测试已通过；完成后必须报告实际工作目录、实际命令、真实结果、修改文件和风险。
项目：食物识别生成菜谱智能体平台，第 2 天，角色：AI / 食物识别模型。
今日目标：在租用 GPU 服务器完成首轮训练。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/黄小石/day2.md`。

## 第 3 天：Mock 全链路

### 今天直接做什么

继续训练并让 Mock 与真实接口形状一致。

### 开始前读取

- 根目录 `AGENTS.md`：先确认工作目录、包管理器、实际命令和 Agent 执行限制。
- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《06-目标检测功能.pdf》YOLO 推理
- 《07-模型训练功能.pdf》训练和评估
- V1 第三、四节

### 固定输入

- 固定图
- 首轮 checkpoint

### P0：今天必须完成

1. 用固定图评估首轮模型。
2. 确保 `recognize(image_path, 0.25)` 返回 list[ModelDetection]。
3. 向绕家辉提供运行时调用说明。

### P1：原则上完成

- 记录错误案例。
- 选择当前最佳 checkpoint。

### P2：有时间再做

- 补充更多固定图。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/scripts/food_model/classes.yaml | 建议新增 | 12 类及中英文映射 |
| backend/scripts/food_model/prepare_dataset.py | 建议新增 | 数据转换和检查 |
| backend/scripts/food_model/train.py | 建议新增 | GPU 服务器训练入口 |
| backend/scripts/food_model/evaluate.py | 建议新增 | 固定图和指标评估 |
| backend/scripts/food_model/README.md | 建议新增 | 文件名、类别顺序、用法和限制 |
| backend/app/modeling/food_yolo_runtime.py | 建议新增 | 统一模型运行时 |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/app/api/food.py | 绕家辉负责 |
| backend/app/entity/db_models.py | 绕家辉负责 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| frontend/ | 不修改前端 |
| docker-compose.yml | 吴雯负责挂载 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- 固定图报告
- 模型运行说明
- 候选 best.pt 本地位置

### 测试和证据

- 空图、无结果图和多食材图各测一次。
- 确认 best.pt 未被 Git 跟踪。
- 把实际命令、结果和截图写入 `docs/daily_logs/黄小石/day3.md`。

### 对接与交付

绕家辉只按接口接入，不介入训练参数。

- 在固定分支 `feature/model-food-yolo` 提交真实改动；个人临时分支通过 PR 合入 `develop`，合并后及时删除。Day3 和 Day4 直接在 `develop` 联调，不再创建额外 integration 分支。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
开始任务前必须先阅读：
1. 根目录 `AGENTS.md`；
2. `docs/contracts/api_v1.md`；
3. 本人的个人任务计划。
`AGENTS.md` 负责环境和命令；`api_v1.md` 负责接口。
后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。
不得声称未执行的测试已通过；完成后必须报告实际工作目录、实际命令、真实结果、修改文件和风险。
项目：食物识别生成菜谱智能体平台，第 3 天，角色：AI / 食物识别模型。
今日目标：继续训练并让 Mock 与真实接口形状一致。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/黄小石/day3.md`。

## 第 4 天：真实模型与真实 LLM

### 今天直接做什么

交付最终真实模型给 Docker 验收环境。

### 开始前读取

- 根目录 `AGENTS.md`：先确认工作目录、包管理器、实际命令和 Agent 执行限制。
- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《06-目标检测功能.pdf》YOLO 推理
- 《07-模型训练功能.pdf》训练和评估
- V1 第三、四节

### 固定输入

- 最终 best.pt
- classes.yaml
- 固定图

### P0：今天必须完成

1. 确定最终 best.pt 并复制到宿主机 `./models/food/best.pt`。
2. 确认容器路径 `/models/food/best.pt`。
3. 用 0.25 阈值跑完固定图并记录结果。

### P1：原则上完成

- 补最终模型限制说明。
- 配合绕家辉验证不可用异常。

### P2：有时间再做

- ONNX 导出仅在主流程稳定后尝试。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/scripts/food_model/classes.yaml | 建议新增 | 12 类及中英文映射 |
| backend/scripts/food_model/prepare_dataset.py | 建议新增 | 数据转换和检查 |
| backend/scripts/food_model/train.py | 建议新增 | GPU 服务器训练入口 |
| backend/scripts/food_model/evaluate.py | 建议新增 | 固定图和指标评估 |
| backend/scripts/food_model/README.md | 建议新增 | 文件名、类别顺序、用法和限制 |
| backend/app/modeling/food_yolo_runtime.py | 建议新增 | 统一模型运行时 |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/app/api/food.py | 绕家辉负责 |
| backend/app/entity/db_models.py | 绕家辉负责 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| frontend/ | 不修改前端 |
| docker-compose.yml | 吴雯负责挂载 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- 最终指标
- 本地加载说明
- 真实接口结果

### 测试和证据

- 真实 Provider 固定图测试。
- 断开权重路径验证 FoodModelUnavailableError。
- 把实际命令、结果和截图写入 `docs/daily_logs/黄小石/day4.md`。

### 对接与交付

把本地文件路径交吴雯，不提交权重。

- 在固定分支 `feature/model-food-yolo` 提交真实改动；个人临时分支通过 PR 合入 `develop`，合并后及时删除。Day3 和 Day4 直接在 `develop` 联调，不再创建额外 integration 分支。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
开始任务前必须先阅读：
1. 根目录 `AGENTS.md`；
2. `docs/contracts/api_v1.md`；
3. 本人的个人任务计划。
`AGENTS.md` 负责环境和命令；`api_v1.md` 负责接口。
后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。
不得声称未执行的测试已通过；完成后必须报告实际工作目录、实际命令、真实结果、修改文件和风险。
项目：食物识别生成菜谱智能体平台，第 4 天，角色：AI / 食物识别模型。
今日目标：交付最终真实模型给 Docker 验收环境。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/黄小石/day4.md`。

## 第 5 天：回归、Docker 与答辩证据

### 今天直接做什么

整理模型证据和答辩说明，不再改类别。

### 开始前读取

- 根目录 `AGENTS.md`：先确认工作目录、包管理器、实际命令和 Agent 执行限制。
- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《06-目标检测功能.pdf》YOLO 推理
- 《07-模型训练功能.pdf》训练和评估
- V1 第三、四节

### 固定输入

- 最终模型
- 吴雯缺陷清单

### P0：今天必须完成

1. 复测固定图。
2. 整理 12 类、指标、限制和回退说明。
3. 完成个人日志和 PPT 素材。

### P1：原则上完成

- 回答吴雯复测问题。
- 核对 Git 中无 *.pt 和数据集。

### P2：有时间再做

- 记录后续优化方向。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/scripts/food_model/classes.yaml | 建议新增 | 12 类及中英文映射 |
| backend/scripts/food_model/prepare_dataset.py | 建议新增 | 数据转换和检查 |
| backend/scripts/food_model/train.py | 建议新增 | GPU 服务器训练入口 |
| backend/scripts/food_model/evaluate.py | 建议新增 | 固定图和指标评估 |
| backend/scripts/food_model/README.md | 建议新增 | 文件名、类别顺序、用法和限制 |
| backend/app/modeling/food_yolo_runtime.py | 建议新增 | 统一模型运行时 |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/app/api/food.py | 绕家辉负责 |
| backend/app/entity/db_models.py | 绕家辉负责 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| frontend/ | 不修改前端 |
| docker-compose.yml | 吴雯负责挂载 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- 模型报告
- 截图
- 五天总结

### 测试和证据

- 固定图最终复测。
- 检查 Git 文件清单不含权重。
- 把实际命令、结果和截图写入 `docs/daily_logs/黄小石/day5.md`。

### 对接与交付

把证据交闫灿宇和吴雯。

- 在固定分支 `feature/model-food-yolo` 提交真实改动；个人临时分支通过 PR 合入 `develop`，合并后及时删除。Day3 和 Day4 直接在 `develop` 联调，不再创建额外 integration 分支。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
开始任务前必须先阅读：
1. 根目录 `AGENTS.md`；
2. `docs/contracts/api_v1.md`；
3. 本人的个人任务计划。
`AGENTS.md` 负责环境和命令；`api_v1.md` 负责接口。
后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。
不得声称未执行的测试已通过；完成后必须报告实际工作目录、实际命令、真实结果、修改文件和风险。
项目：食物识别生成菜谱智能体平台，第 5 天，角色：AI / 食物识别模型。
今日目标：整理模型证据和答辩说明，不再改类别。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/黄小石/day5.md`。

## 五天结束时必须交付

- 负责模块的源码和测试。
- day1 到 day5 个人日志。
- Commit、PR 或 Draft PR 记录。
- 页面、API、模型、SSE 或 Docker 证据。
- 能用自己的话说明输入、输出、错误处理和本人修改内容。

## 答辩时必须会讲

- 自己的角色和文件边界。
- V1 中与自己有关的固定输入输出。
- 自己实际运行的测试和结果。
- AI 工具做了什么、本人检查和修改了什么。
- 未完成或降级到 P2 的内容。
