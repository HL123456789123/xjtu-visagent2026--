# 吴雯：五天个人任务计划（V1 执行版）

> 角色：测试、Docker 与部署
> 唯一接口标准：`docs/contracts/api_v1.md`。发现冲突只提交问题，不得私自修改对外字段；模块内部辅助字段允许增加。

## 开始前统一原则

根目录 `AGENTS.md` 负责环境和命令，`docs/contracts/api_v1.md` 负责接口，本计划只负责任务、Owner、文件边界、优先级和交付物。后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。本文保留的命令只作任务验收提示，工作目录和最终命令以 `AGENTS.md` 为准；不得声称未执行的测试已通过，完成后必须报告实际工作目录、命令、结果和风险。

Day3 和 Day4 直接在 `develop` 联调，用集成记录和对应 Commit 哈希标记阶段状态；不创建额外 `integration/*` 分支。个人临时分支通过 PR 合入 `develop` 后及时删除，`master` 只用于最终稳定版本。

已有代码先运行和核对，不直接推倒重写。对外接口与 V1 不一致时优先增加 Adapter；创新只能放在模块内部，必须可关闭或回滚，且不得阻塞 Day3/Day4。需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。

## 1. 我的总体职责

用 canonical fixture 和契约测试阻止字段漂移，并完成两轮 Docker 验收。

- 按 V1 创建四个 canonical fixture。
- 编写 Food、Recipe、Chat 和 SSE 契约测试。
- 发现字段不一致直接报错，不写多版本兼容层。
- 维护 Compose、Dockerfile、Nginx 和健康检查。
- Day3 跑 Mock E2E，Day4 跑真实模型/LLM E2E。
- Day5 给出 P0 是否清零和是否允许发布的结论。

## 2. 我负责的文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/tests/fixtures/ | 建议新增 | 四个 canonical fixture |
| backend/tests/test_food_contract.py | 建议新增 | Food 契约测试 |
| backend/tests/test_recipe_contract.py | 建议新增 | Recipe 契约测试 |
| backend/tests/test_chat_sse_contract.py | 建议新增 | 四类 SSE 测试 |
| docker-compose.yml | 现有 | 服务、权重挂载和环境变量 |
| backend/Dockerfile | 现有 | 后端镜像 |
| frontend/Dockerfile | 现有 | 前端镜像 |
| frontend/nginx.conf | 现有 | 代理和 SSE |

## 3. 我不能直接修改的文件

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/main.py | 闫灿宇负责 |
| backend/app/entity/db_models.py | 绕家辉负责 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| frontend/src/views/FoodRecipePage.vue | 刘楚涵负责 |
| frontend/src/views/ChatPage.vue | 李晨宁负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

## 4. 我必须遵守的接口

- V1 全字段
- canonical fixtures
- HTTP 状态码
- 四类 SSE
- Docker 健康状态

## 5. 五天总览

| 天数 | 主题 | 我的关键输出 |
| --- | --- | --- |
| 第 1 天 | 领取冻结接口，建立模块骨架 | 四个 fixture、契约测试、环境记录 |
| 第 2 天 | 按固定输入输出完成核心模块 | Day2 测试报告、Docker 阻断清单 |
| 第 3 天 | Mock 全链路 | Mock E2E 报告、缺陷单、截图 |
| 第 4 天 | 真实模型与真实 LLM | 真实 E2E 报告、503 报告、Docker 日志 |
| 第 5 天 | 回归、Docker 与答辩证据 | 最终测试结论、证据索引、发布建议 |

## 第 1 天：领取冻结接口，建立模块骨架

### 今天直接做什么

先提交 canonical fixture，并用它检查所有骨架。

### 开始前读取

- 根目录 `AGENTS.md`：先确认工作目录、包管理器、实际命令和 Agent 执行限制。
- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《10-日志监控与单元测试.pdf》测试和日志
- 《11-Docker容器化部署.pdf》Compose 与 Nginx
- V1 全文

### 固定输入

- V1 全文
- 现有测试框架

### P0：今天必须完成

1. 建立四个 V1 fixture。
2. 建立 Food、Recipe、SSE 契约测试骨架。
3. 检查所有成员引用 `docs/contracts/api_v1.md`。

### P1：原则上完成

- 记录环境和 Docker 基线。
- 建立缺陷模板。

### P2：有时间再做

- 补测试报告模板。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/tests/fixtures/ | 建议新增 | 四个 canonical fixture |
| backend/tests/test_food_contract.py | 建议新增 | Food 契约测试 |
| backend/tests/test_recipe_contract.py | 建议新增 | Recipe 契约测试 |
| backend/tests/test_chat_sse_contract.py | 建议新增 | 四类 SSE 测试 |
| docker-compose.yml | 现有 | 服务、权重挂载和环境变量 |
| backend/Dockerfile | 现有 | 后端镜像 |
| frontend/Dockerfile | 现有 | 前端镜像 |
| frontend/nginx.conf | 现有 | 代理和 SSE |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/main.py | 闫灿宇负责 |
| backend/app/entity/db_models.py | 绕家辉负责 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| frontend/src/views/FoodRecipePage.vue | 刘楚涵负责 |
| frontend/src/views/ChatPage.vue | 李晨宁负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- 四个 fixture
- 契约测试
- 环境记录

### 测试和证据

- `uv run pytest`。
- fixture 字段与 V1 逐项比较。
- 把实际命令、结果和截图写入 `docs/daily_logs/吴雯/day1.md`。

### 对接与交付

不一致直接报错并退回 owner，不讨论另一套结构。

- 在固定分支 `chore/test-docker` 提交真实改动；个人临时分支通过 PR 合入 `develop`，合并后及时删除。Day3 和 Day4 直接在 `develop` 联调，不再创建额外 integration 分支。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
开始任务前必须先阅读：
1. 根目录 `AGENTS.md`；
2. `docs/contracts/api_v1.md`；
3. 本人的个人任务计划。
`AGENTS.md` 负责环境和命令；`api_v1.md` 负责接口。
后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。
不得声称未执行的测试已通过；完成后必须报告实际工作目录、实际命令、真实结果、修改文件和风险。
项目：食物识别生成菜谱智能体平台，第 1 天，角色：测试、Docker 与部署。
今日目标：先提交 canonical fixture，并用它检查所有骨架。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/吴雯/day1.md`。

## 第 2 天：按固定输入输出完成核心模块

### 今天直接做什么

让核心模块通过契约测试，并解决 Docker 阻断。

### 开始前读取

- 根目录 `AGENTS.md`：先确认工作目录、包管理器、实际命令和 Agent 执行限制。
- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《10-日志监控与单元测试.pdf》测试和日志
- 《11-Docker容器化部署.pdf》Compose 与 Nginx
- V1 全文

### 固定输入

- 各模块 PR
- canonical fixtures

### P0：今天必须完成

1. 测试三条 Food API。
2. 测试两条 Recipe API 和两条 Chat API。
3. 检查 Docker 配置、PostgreSQL、Redis、MinIO 和 Nginx。

### P1：原则上完成

- 补用户隔离测试。
- 维护缺陷清单。

### P2：有时间再做

- 补复杂异常用例。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/tests/fixtures/ | 建议新增 | 四个 canonical fixture |
| backend/tests/test_food_contract.py | 建议新增 | Food 契约测试 |
| backend/tests/test_recipe_contract.py | 建议新增 | Recipe 契约测试 |
| backend/tests/test_chat_sse_contract.py | 建议新增 | 四类 SSE 测试 |
| docker-compose.yml | 现有 | 服务、权重挂载和环境变量 |
| backend/Dockerfile | 现有 | 后端镜像 |
| frontend/Dockerfile | 现有 | 前端镜像 |
| frontend/nginx.conf | 现有 | 代理和 SSE |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/main.py | 闫灿宇负责 |
| backend/app/entity/db_models.py | 绕家辉负责 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| frontend/src/views/FoodRecipePage.vue | 刘楚涵负责 |
| frontend/src/views/ChatPage.vue | 李晨宁负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- Day2 测试报告
- Docker 阻断清单

### 测试和证据

- `uv run pytest`、`bun run test`、`docker compose config`。
- 错误码按 V1 验证。
- 把实际命令、结果和截图写入 `docs/daily_logs/吴雯/day2.md`。

### 对接与交付

只让引入问题的 owner 修复。

- 在固定分支 `chore/test-docker` 提交真实改动；个人临时分支通过 PR 合入 `develop`，合并后及时删除。Day3 和 Day4 直接在 `develop` 联调，不再创建额外 integration 分支。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
开始任务前必须先阅读：
1. 根目录 `AGENTS.md`；
2. `docs/contracts/api_v1.md`；
3. 本人的个人任务计划。
`AGENTS.md` 负责环境和命令；`api_v1.md` 负责接口。
后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。
不得声称未执行的测试已通过；完成后必须报告实际工作目录、实际命令、真实结果、修改文件和风险。
项目：食物识别生成菜谱智能体平台，第 2 天，角色：测试、Docker 与部署。
今日目标：让核心模块通过契约测试，并解决 Docker 阻断。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/吴雯/day2.md`。

## 第 3 天：Mock 全链路

### 今天直接做什么

直接在 `develop` 跑完整 Day3 Mock E2E，并记录联调 Commit 哈希。

### 开始前读取

- 根目录 `AGENTS.md`：先确认工作目录、包管理器、实际命令和 Agent 执行限制。
- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《10-日志监控与单元测试.pdf》测试和日志
- 《11-Docker容器化部署.pdf》Compose 与 Nginx
- V1 全文

### 固定输入

- Mock Provider
- Fake LLM
- canonical fixtures

### P0：今天必须完成

1. 跑登录、上传、编辑、确认、生成、展示。
2. 跑 Fake 问答和 update_recipe。
3. 验证四类 SSE 和重新 GET。

### P1：原则上完成

- 验证用户隔离。
- 保存截图和日志。

### P2：有时间再做

- 增加一个断流用例。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/tests/fixtures/ | 建议新增 | 四个 canonical fixture |
| backend/tests/test_food_contract.py | 建议新增 | Food 契约测试 |
| backend/tests/test_recipe_contract.py | 建议新增 | Recipe 契约测试 |
| backend/tests/test_chat_sse_contract.py | 建议新增 | 四类 SSE 测试 |
| docker-compose.yml | 现有 | 服务、权重挂载和环境变量 |
| backend/Dockerfile | 现有 | 后端镜像 |
| frontend/Dockerfile | 现有 | 前端镜像 |
| frontend/nginx.conf | 现有 | 代理和 SSE |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/main.py | 闫灿宇负责 |
| backend/app/entity/db_models.py | 绕家辉负责 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| frontend/src/views/FoodRecipePage.vue | 刘楚涵负责 |
| frontend/src/views/ChatPage.vue | 李晨宁负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- Mock E2E 报告
- 缺陷单
- 截图

### 测试和证据

- 按 V1 最终验收 1 到 12。
- 前后端测试全跑。
- 把实际命令、结果和截图写入 `docs/daily_logs/吴雯/day3.md`。

### 对接与交付

P0 不通过时 develop 不结束当天集成。

- 在固定分支 `chore/test-docker` 提交真实改动；个人临时分支通过 PR 合入 `develop`，合并后及时删除。Day3 和 Day4 直接在 `develop` 联调，不再创建额外 integration 分支。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
开始任务前必须先阅读：
1. 根目录 `AGENTS.md`；
2. `docs/contracts/api_v1.md`；
3. 本人的个人任务计划。
`AGENTS.md` 负责环境和命令；`api_v1.md` 负责接口。
后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。
不得声称未执行的测试已通过；完成后必须报告实际工作目录、实际命令、真实结果、修改文件和风险。
项目：食物识别生成菜谱智能体平台，第 3 天，角色：测试、Docker 与部署。
今日目标：直接在 `develop` 跑完整 Day3 Mock E2E，并记录联调 Commit 哈希。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/吴雯/day3.md`。

## 第 4 天：真实模型与真实 LLM

### 今天直接做什么

直接在 `develop` 和 Docker 中跑 Day4 真实 E2E，并记录联调 Commit 哈希。

### 开始前读取

- 根目录 `AGENTS.md`：先确认工作目录、包管理器、实际命令和 Agent 执行限制。
- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《10-日志监控与单元测试.pdf》测试和日志
- 《11-Docker容器化部署.pdf》Compose 与 Nginx
- V1 全文

### 固定输入

- best.pt 本地文件
- 真实 LLM 配置
- Docker

### P0：今天必须完成

1. 验证 12 类真实 YOLO 和固定图。
2. 验证真实 LLM 生成/修改或明确 503。
3. 验证 version=2、recipe_updated 和重新 GET。

### P1：原则上完成

- 验证模型权重挂载。
- 登记全部阻断缺陷。

### P2：有时间再做

- 记录基础性能。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/tests/fixtures/ | 建议新增 | 四个 canonical fixture |
| backend/tests/test_food_contract.py | 建议新增 | Food 契约测试 |
| backend/tests/test_recipe_contract.py | 建议新增 | Recipe 契约测试 |
| backend/tests/test_chat_sse_contract.py | 建议新增 | 四类 SSE 测试 |
| docker-compose.yml | 现有 | 服务、权重挂载和环境变量 |
| backend/Dockerfile | 现有 | 后端镜像 |
| frontend/Dockerfile | 现有 | 前端镜像 |
| frontend/nginx.conf | 现有 | 代理和 SSE |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/main.py | 闫灿宇负责 |
| backend/app/entity/db_models.py | 绕家辉负责 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| frontend/src/views/FoodRecipePage.vue | 刘楚涵负责 |
| frontend/src/views/ChatPage.vue | 李晨宁负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- 真实 E2E 报告
- 503 报告
- Docker 日志

### 测试和证据

- `docker compose up -d`。
- 完整验收流程再跑一次。
- 把实际命令、结果和截图写入 `docs/daily_logs/吴雯/day4.md`。

### 对接与交付

当天把 P0 分配给对应 owner。

- 在固定分支 `chore/test-docker` 提交真实改动；个人临时分支通过 PR 合入 `develop`，合并后及时删除。Day3 和 Day4 直接在 `develop` 联调，不再创建额外 integration 分支。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
开始任务前必须先阅读：
1. 根目录 `AGENTS.md`；
2. `docs/contracts/api_v1.md`；
3. 本人的个人任务计划。
`AGENTS.md` 负责环境和命令；`api_v1.md` 负责接口。
后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。
不得声称未执行的测试已通过；完成后必须报告实际工作目录、实际命令、真实结果、修改文件和风险。
项目：食物识别生成菜谱智能体平台，第 4 天，角色：测试、Docker 与部署。
今日目标：直接在 `develop` 和 Docker 中跑 Day4 真实 E2E，并记录联调 Commit 哈希。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/吴雯/day4.md`。

## 第 5 天：回归、Docker 与答辩证据

### 今天直接做什么

完成最终回归并给出是否发布结论。

### 开始前读取

- 根目录 `AGENTS.md`：先确认工作目录、包管理器、实际命令和 Agent 执行限制。
- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《10-日志监控与单元测试.pdf》测试和日志
- 《11-Docker容器化部署.pdf》Compose 与 Nginx
- V1 全文

### 固定输入

- release candidate
- 全部证据

### P0：今天必须完成

1. 全量测试和 Docker 主流程。
2. 确认 P0 清零、P1 有结论。
3. 整理证据索引和最终验收报告。

### P1：原则上完成

- 参与两轮 PPT 彩排。
- 复核七人日志。

### P2：有时间再做

- 不增加新测试框架。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| backend/tests/fixtures/ | 建议新增 | 四个 canonical fixture |
| backend/tests/test_food_contract.py | 建议新增 | Food 契约测试 |
| backend/tests/test_recipe_contract.py | 建议新增 | Recipe 契约测试 |
| backend/tests/test_chat_sse_contract.py | 建议新增 | 四类 SSE 测试 |
| docker-compose.yml | 现有 | 服务、权重挂载和环境变量 |
| backend/Dockerfile | 现有 | 后端镜像 |
| frontend/Dockerfile | 现有 | 前端镜像 |
| frontend/nginx.conf | 现有 | 代理和 SSE |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| backend/main.py | 闫灿宇负责 |
| backend/app/entity/db_models.py | 绕家辉负责 |
| backend/app/services/agent_graph.py | 陈煜君负责 |
| frontend/src/views/FoodRecipePage.vue | 刘楚涵负责 |
| frontend/src/views/ChatPage.vue | 李晨宁负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- 最终测试结论
- 证据索引
- 发布建议

### 测试和证据

- `uv run pytest`、`bun run test`、`bun run build`。
- Docker 主流程和 503 场景。
- 把实际命令、结果和截图写入 `docs/daily_logs/吴雯/day5.md`。

### 对接与交付

测试通过后交闫灿宇确认发布。

- 在固定分支 `chore/test-docker` 提交真实改动；个人临时分支通过 PR 合入 `develop`，合并后及时删除。Day3 和 Day4 直接在 `develop` 联调，不再创建额外 integration 分支。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
开始任务前必须先阅读：
1. 根目录 `AGENTS.md`；
2. `docs/contracts/api_v1.md`；
3. 本人的个人任务计划。
`AGENTS.md` 负责环境和命令；`api_v1.md` 负责接口。
后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。
不得声称未执行的测试已通过；完成后必须报告实际工作目录、实际命令、真实结果、修改文件和风险。
项目：食物识别生成菜谱智能体平台，第 5 天，角色：测试、Docker 与部署。
今日目标：完成最终回归并给出是否发布结论。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/吴雯/day5.md`。

## 五天结束时必须交付

- 负责模块的源码和测试。
- day1 到 day5 个人日志。
- Commit、PR 或 Draft PR 记录。
- 页面、API、模型、SSE 或 Docker 证据。
- 能用自己的话说明输入、输出、错误处理和本人修改内容。

## 答辩时必须会讲

- 自己的角色和文件边界。
- V1 中与自己有关的固定输入输出。
- 自己实际运行的测试和结果。
- AI 工具做了什么、本人检查和修改了什么。
- 未完成或降级到 P2 的内容。
