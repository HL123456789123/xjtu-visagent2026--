# 李晨宁：五天个人任务计划（V1 执行版）

> 角色：菜谱展示与智能对话前端
> 唯一接口标准：`docs/contracts/api_v1.md`。发现冲突只提交问题，不得私自修改对外字段；模块内部辅助字段允许增加。

## 开始前统一原则

根目录 `AGENTS.md` 负责环境和命令，`docs/contracts/api_v1.md` 负责接口，本计划只负责任务、Owner、文件边界、优先级和交付物。后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。本文保留的命令只作任务验收提示，工作目录和最终命令以 `AGENTS.md` 为准；不得声称未执行的测试已通过，完成后必须报告实际工作目录、命令、结果和风险。

Day3 和 Day4 直接在 `develop` 联调，用集成记录和对应 Commit 哈希标记阶段状态；不创建额外 `integration/*` 分支。个人临时分支通过 PR 合入 `develop` 后及时删除，`master` 只用于最终稳定版本。

已有代码先运行和核对，不直接推倒重写。对外接口与 V1 不一致时优先增加 Adapter；创新只能放在模块内部，必须可关闭或回滚，且不得阻塞 Day3/Day4。需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。

## 1. 我的总体职责

按固定 Recipe JSON 展示菜谱，并只解析 V1 规定的四类 SSE 事件。

- 封装 `frontend/src/api/recipe.js` 和复用 `frontend/src/api/chat.js`。
- 展示 title、summary、servings、cooking_time_minutes、difficulty、ingredients、steps 和 nutrition。
- 始终显示固定营养免责声明。
- Chat 只解析 `token`、`recipe_updated`、`done`、`error`。
- 收到 `recipe_updated` 后重新 GET 菜谱。
- 503 时显示明确不可用提示。

## 2. 我负责的文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| frontend/src/api/recipe.js | 建议新增 | V1 Recipe API |
| frontend/src/api/chat.js | 现有 | 按 V1 请求体调整 |
| frontend/src/components/recipe/ | 建议新增 | 菜谱展示组件 |
| frontend/src/views/ChatPage.vue | 现有 | 聊天和刷新 |
| frontend/src/utils/stream.js | 现有 | 四类 SSE 解析 |

## 3. 我不能直接修改的文件

| 不能直接修改 | 原因或 owner |
| --- | --- |
| frontend/src/router/index.js | 闫灿宇接线 |
| frontend/src/components/layout/AppSidebar.vue | 闫灿宇接线 |
| frontend/src/views/FoodRecipePage.vue | 刘楚涵负责 |
| backend/ | 不修改后端 |
| docker-compose.yml | 吴雯负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

## 4. 我必须遵守的接口

- Recipe JSON
- POST Chat session/message
- token
- recipe_updated
- done
- error

## 5. 五天总览

| 天数 | 主题 | 我的关键输出 |
| --- | --- | --- |
| 第 1 天 | 领取冻结接口，建立模块骨架 | Recipe 组件、四类 SSE parser、Day1 截图 |
| 第 2 天 | 按固定输入输出完成核心模块 | 完整菜谱卡片、Chat API 封装 |
| 第 3 天 | Mock 全链路 | Mock 菜谱和聊天页面、刷新截图 |
| 第 4 天 | 真实模型与真实 LLM | version=2 页面、503 页面、Day4 日志 |
| 第 5 天 | 回归、Docker 与答辩证据 | 最终截图、测试结果、五天总结 |

## 第 1 天：领取冻结接口，建立模块骨架

### 今天直接做什么

用固定 Recipe fixture 建展示组件和四类 SSE 解析器。

### 开始前读取

- 根目录 `AGENTS.md`：先确认工作目录、包管理器、实际命令和 Agent 执行限制。
- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《04-前端项目初始化.pdf》组件和状态
- 《09-AI智能对话功能.pdf》SSE
- V1 第六、八、十三节

### 固定输入

- recipe_success.json
- sse_recipe_update.txt

### P0：今天必须完成

1. 建立 `frontend/src/api/recipe.js`。
2. 建立 title、ingredients、steps、nutrition 组件。
3. stream.js 只识别 token、recipe_updated、done、error。

### P1：原则上完成

- 固定显示营养免责声明。
- 建立 503 错误占位。

### P2：有时间再做

- 基础样式。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| frontend/src/api/recipe.js | 建议新增 | V1 Recipe API |
| frontend/src/api/chat.js | 现有 | 按 V1 请求体调整 |
| frontend/src/components/recipe/ | 建议新增 | 菜谱展示组件 |
| frontend/src/views/ChatPage.vue | 现有 | 聊天和刷新 |
| frontend/src/utils/stream.js | 现有 | 四类 SSE 解析 |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| frontend/src/router/index.js | 闫灿宇接线 |
| frontend/src/components/layout/AppSidebar.vue | 闫灿宇接线 |
| frontend/src/views/FoodRecipePage.vue | 刘楚涵负责 |
| backend/ | 不修改后端 |
| docker-compose.yml | 吴雯负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- Recipe 组件
- 四类 SSE parser
- Day1 截图

### 测试和证据

- `bun run test`。
- 遇到其他 event 必须按未知事件处理，不能新增兼容。
- 把实际命令、结果和截图写入 `docs/daily_logs/李晨宁/day1.md`。

### 对接与交付

路由需求交闫灿宇。

- 在固定分支 `feature/frontend-recipe-chat` 提交真实改动；个人临时分支通过 PR 合入 `develop`，合并后及时删除。Day3 和 Day4 直接在 `develop` 联调，不再创建额外 integration 分支。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
开始任务前必须先阅读：
1. 根目录 `AGENTS.md`；
2. `docs/contracts/api_v1.md`；
3. 本人的个人任务计划。
`AGENTS.md` 负责环境和命令；`api_v1.md` 负责接口。
后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。
不得声称未执行的测试已通过；完成后必须报告实际工作目录、实际命令、真实结果、修改文件和风险。
项目：食物识别生成菜谱智能体平台，第 1 天，角色：菜谱展示与智能对话前端。
今日目标：用固定 Recipe fixture 建展示组件和四类 SSE 解析器。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/李晨宁/day1.md`。

## 第 2 天：按固定输入输出完成核心模块

### 今天直接做什么

完成固定 Recipe JSON 展示和 Chat 请求封装。

### 开始前读取

- 根目录 `AGENTS.md`：先确认工作目录、包管理器、实际命令和 Agent 执行限制。
- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《04-前端项目初始化.pdf》组件和状态
- 《09-AI智能对话功能.pdf》SSE
- V1 第六、八、十三节

### 固定输入

- Recipe API fixture
- Chat fixture

### P0：今天必须完成

1. 展示 cooking_time_minutes 和 carbohydrates_g 等 V1 字段。
2. 创建会话请求只传 recipe_id。
3. 发送消息请求只传 content。

### P1：原则上完成

- 处理 Loading、空数据和 503。
- 补组件测试。

### P2：有时间再做

- 优化步骤样式。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| frontend/src/api/recipe.js | 建议新增 | V1 Recipe API |
| frontend/src/api/chat.js | 现有 | 按 V1 请求体调整 |
| frontend/src/components/recipe/ | 建议新增 | 菜谱展示组件 |
| frontend/src/views/ChatPage.vue | 现有 | 聊天和刷新 |
| frontend/src/utils/stream.js | 现有 | 四类 SSE 解析 |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| frontend/src/router/index.js | 闫灿宇接线 |
| frontend/src/components/layout/AppSidebar.vue | 闫灿宇接线 |
| frontend/src/views/FoodRecipePage.vue | 刘楚涵负责 |
| backend/ | 不修改后端 |
| docker-compose.yml | 吴雯负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- 完整菜谱卡片
- Chat API 封装

### 测试和证据

- `bun run test` 和 `bun run build`。
- 固定 JSON 每个字段都能显示。
- 把实际命令、结果和截图写入 `docs/daily_logs/李晨宁/day2.md`。

### 对接与交付

把字段问题提交给原后端 owner，不自行改名。

- 在固定分支 `feature/frontend-recipe-chat` 提交真实改动；个人临时分支通过 PR 合入 `develop`，合并后及时删除。Day3 和 Day4 直接在 `develop` 联调，不再创建额外 integration 分支。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
开始任务前必须先阅读：
1. 根目录 `AGENTS.md`；
2. `docs/contracts/api_v1.md`；
3. 本人的个人任务计划。
`AGENTS.md` 负责环境和命令；`api_v1.md` 负责接口。
后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。
不得声称未执行的测试已通过；完成后必须报告实际工作目录、实际命令、真实结果、修改文件和风险。
项目：食物识别生成菜谱智能体平台，第 2 天，角色：菜谱展示与智能对话前端。
今日目标：完成固定 Recipe JSON 展示和 Chat 请求封装。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/李晨宁/day2.md`。

## 第 3 天：Mock 全链路

### 今天直接做什么

接 Fake Recipe/Chat，完成 Mock 刷新。

### 开始前读取

- 根目录 `AGENTS.md`：先确认工作目录、包管理器、实际命令和 Agent 执行限制。
- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《04-前端项目初始化.pdf》组件和状态
- 《09-AI智能对话功能.pdf》SSE
- V1 第六、八、十三节

### 固定输入

- Fake Recipe API
- 四类 SSE

### P0：今天必须完成

1. 从 recognition_id 生成并查询菜谱。
2. 发送 Fake 对话修改。
3. 收到 recipe_updated 后重新 GET 并显示 version=2。

### P1：原则上完成

- 处理 done 和 error。
- 配合吴雯跑 E2E。

### P2：有时间再做

- 补断流提示。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| frontend/src/api/recipe.js | 建议新增 | V1 Recipe API |
| frontend/src/api/chat.js | 现有 | 按 V1 请求体调整 |
| frontend/src/components/recipe/ | 建议新增 | 菜谱展示组件 |
| frontend/src/views/ChatPage.vue | 现有 | 聊天和刷新 |
| frontend/src/utils/stream.js | 现有 | 四类 SSE 解析 |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| frontend/src/router/index.js | 闫灿宇接线 |
| frontend/src/components/layout/AppSidebar.vue | 闫灿宇接线 |
| frontend/src/views/FoodRecipePage.vue | 刘楚涵负责 |
| backend/ | 不修改后端 |
| docker-compose.yml | 吴雯负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- Mock 菜谱和聊天页面
- 刷新截图

### 测试和证据

- `bun run test`。
- answer 不刷新，recipe_updated 才重新 GET。
- 把实际命令、结果和截图写入 `docs/daily_logs/李晨宁/day3.md`。

### 对接与交付

把结果交闫灿宇和吴雯。

- 在固定分支 `feature/frontend-recipe-chat` 提交真实改动；个人临时分支通过 PR 合入 `develop`，合并后及时删除。Day3 和 Day4 直接在 `develop` 联调，不再创建额外 integration 分支。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
开始任务前必须先阅读：
1. 根目录 `AGENTS.md`；
2. `docs/contracts/api_v1.md`；
3. 本人的个人任务计划。
`AGENTS.md` 负责环境和命令；`api_v1.md` 负责接口。
后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。
不得声称未执行的测试已通过；完成后必须报告实际工作目录、实际命令、真实结果、修改文件和风险。
项目：食物识别生成菜谱智能体平台，第 3 天，角色：菜谱展示与智能对话前端。
今日目标：接 Fake Recipe/Chat，完成 Mock 刷新。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/李晨宁/day3.md`。

## 第 4 天：真实模型与真实 LLM

### 今天直接做什么

接真实 Recipe/Chat，验证真实更新或 503。

### 开始前读取

- 根目录 `AGENTS.md`：先确认工作目录、包管理器、实际命令和 Agent 执行限制。
- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《04-前端项目初始化.pdf》组件和状态
- 《09-AI智能对话功能.pdf》SSE
- V1 第六、八、十三节

### 固定输入

- 真实 Recipe API
- 真实四类 SSE

### P0：今天必须完成

1. 显示真实生成菜谱。
2. 发送‘改成三人份并少放油’。
3. 收到 recipe_updated 后 GET version=2。

### P1：原则上完成

- 保存真实页面截图。
- 验证 LLM 503 文案。

### P2：有时间再做

- 记录 SSE 延迟。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| frontend/src/api/recipe.js | 建议新增 | V1 Recipe API |
| frontend/src/api/chat.js | 现有 | 按 V1 请求体调整 |
| frontend/src/components/recipe/ | 建议新增 | 菜谱展示组件 |
| frontend/src/views/ChatPage.vue | 现有 | 聊天和刷新 |
| frontend/src/utils/stream.js | 现有 | 四类 SSE 解析 |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| frontend/src/router/index.js | 闫灿宇接线 |
| frontend/src/components/layout/AppSidebar.vue | 闫灿宇接线 |
| frontend/src/views/FoodRecipePage.vue | 刘楚涵负责 |
| backend/ | 不修改后端 |
| docker-compose.yml | 吴雯负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- version=2 页面
- 503 页面
- Day4 日志

### 测试和证据

- `bun run build`。
- Docker 中完成真实链路。
- 把实际命令、结果和截图写入 `docs/daily_logs/李晨宁/day4.md`。

### 对接与交付

把截图交吴雯和闫灿宇。

- 在固定分支 `feature/frontend-recipe-chat` 提交真实改动；个人临时分支通过 PR 合入 `develop`，合并后及时删除。Day3 和 Day4 直接在 `develop` 联调，不再创建额外 integration 分支。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
开始任务前必须先阅读：
1. 根目录 `AGENTS.md`；
2. `docs/contracts/api_v1.md`；
3. 本人的个人任务计划。
`AGENTS.md` 负责环境和命令；`api_v1.md` 负责接口。
后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。
不得声称未执行的测试已通过；完成后必须报告实际工作目录、实际命令、真实结果、修改文件和风险。
项目：食物识别生成菜谱智能体平台，第 4 天，角色：菜谱展示与智能对话前端。
今日目标：接真实 Recipe/Chat，验证真实更新或 503。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/李晨宁/day4.md`。

## 第 5 天：回归、Docker 与答辩证据

### 今天直接做什么

修复 Recipe/Chat 前端问题并整理证据。

### 开始前读取

- 根目录 `AGENTS.md`：先确认工作目录、包管理器、实际命令和 Agent 执行限制。
- `docs/contracts/api_v1.md`：只读与本人任务相关章节。
- 《04-前端项目初始化.pdf》组件和状态
- 《09-AI智能对话功能.pdf》SSE
- V1 第六、八、十三节

### 固定输入

- release candidate
- 缺陷清单

### P0：今天必须完成

1. 回归展示、问答、修改和错误提示。
2. 修复分配的 P0/P1。
3. 整理刷新和免责声明证据。

### P1：原则上完成

- 完成个人总结。
- 准备答辩讲解。

### P2：有时间再做

- 不增加新事件或页面。

### 负责文件

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| frontend/src/api/recipe.js | 建议新增 | V1 Recipe API |
| frontend/src/api/chat.js | 现有 | 按 V1 请求体调整 |
| frontend/src/components/recipe/ | 建议新增 | 菜谱展示组件 |
| frontend/src/views/ChatPage.vue | 现有 | 聊天和刷新 |
| frontend/src/utils/stream.js | 现有 | 四类 SSE 解析 |

### 不能直接修改

| 不能直接修改 | 原因或 owner |
| --- | --- |
| frontend/src/router/index.js | 闫灿宇接线 |
| frontend/src/components/layout/AppSidebar.vue | 闫灿宇接线 |
| frontend/src/views/FoodRecipePage.vue | 刘楚涵负责 |
| backend/ | 不修改后端 |
| docker-compose.yml | 吴雯负责 |
| V1 对外请求、响应、模型输出、Recipe JSON、状态码和 SSE 事件 | 发现冲突只提交问题，不得私自修改；模块内部辅助字段允许增加，但不得暴露为第二套公开接口 |

### 固定输出与完成现象

- 最终截图
- 测试结果
- 五天总结

### 测试和证据

- `bun run test` 和 `bun run build`。
- Docker 页面回归。
- 把实际命令、结果和截图写入 `docs/daily_logs/李晨宁/day5.md`。

### 对接与交付

复测结果交吴雯。

- 在固定分支 `feature/frontend-recipe-chat` 提交真实改动；个人临时分支通过 PR 合入 `develop`，合并后及时删除。Day3 和 Day4 直接在 `develop` 联调，不再创建额外 integration 分支。
- 使用中文 Conventional Commit。达到可评审状态提交 PR 到 `develop`；未完成可用 Draft PR；不建空 PR。
- 发现契约不一致时在 PR 中指出 V1 章节和实际差异，交原负责人修复，不得私自修改对外字段。

### 可交给 Codex/Qoder 的固定说明

```text
开始任务前必须先阅读：
1. 根目录 `AGENTS.md`；
2. `docs/contracts/api_v1.md`；
3. 本人的个人任务计划。
`AGENTS.md` 负责环境和命令；`api_v1.md` 负责接口。
后端只使用 `uv`，前端只使用 `bun`；不得擅自切换包管理器，不得删除或重建锁文件。
不得声称未执行的测试已通过；完成后必须报告实际工作目录、实际命令、真实结果、修改文件和风险。
项目：食物识别生成菜谱智能体平台，第 5 天，角色：菜谱展示与智能对话前端。
今日目标：修复 Recipe/Chat 前端问题并整理证据。
原则上只修改上方‘负责文件’中与今日 P0 直接相关的文件；需要修改共享文件时，先向 owner 提交接线需求或取得明确授权。
对外输入输出、字段名、路径、状态码和 SSE 事件必须完全按 V1，不得私自增加或修改；模块内部辅助字段允许增加。
已有代码先检查能否保留内部实现；对外不一致时优先增加 Adapter，不要直接推倒重写。
使用 canonical fixture 开发并运行上方测试。发现不一致时报告具体差异，不要修改契约。
完成后列出修改文件、测试命令、实际结果和风险。不要执行 Push、合并或权限设置。
不得提交 API Key、.env、best.pt 或大型数据集。
```

### 个人日志重点

记录今天实际完成、未完成、测试结果、Commit/PR、AI 帮助内容和本人检查过程。日志路径：`docs/daily_logs/李晨宁/day5.md`。

## 五天结束时必须交付

- 负责模块的源码和测试。
- day1 到 day5 个人日志。
- Commit、PR 或 Draft PR 记录。
- 页面、API、模型、SSE 或 Docker 证据。
- 能用自己的话说明输入、输出、错误处理和本人修改内容。

## 答辩时必须会讲

- 自己的角色和文件边界。
- V1 中与自己有关的固定输入输出。
- 自己实际运行的测试和结果。
- AI 工具做了什么、本人检查和修改了什么。
- 未完成或降级到 P2 的内容。
